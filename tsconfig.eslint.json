{
	"extends": "./tsconfig.json",
	"include": [
		// From: https://bobbyhadz.com/blog/typescript-parseroptions-project-has-been-set-for
		// 👇️ add all the directories and files
		// that you want to lint here
		"src",
		"tests",

		// add all files in which you see
		// the "parserOptions.project" error
		".eslintrc.js",
		"*.config.js",
		"*.config.ts",
		".eslintrc.cjs",
		"./playwright/index.ts"
	]
}
